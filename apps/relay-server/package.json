{
  "name": "@another-workbench/relay-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src", "dist"],
  "scripts": {
    "build": "pnpm exec tsc -b tsconfig.json",
    "typecheck": "pnpm exec tsc -b tsconfig.json --pretty false",
    "lint": "node -e \"console.log('lint placeholder: @another-workbench/relay-server')\"",
    "test": "pnpm exec vitest run",
    "start": "pnpm exec tsx src/index.ts"
  },
  "dependencies": {
    "@another-workbench/shared": "workspace:*"
  }
}
