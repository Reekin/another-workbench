{
  "name": "@another-workbench/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src", "dist"],
  "scripts": {
    "build": "pnpm exec tsc -b tsconfig.json",
    "typecheck": "pnpm exec tsc -b tsconfig.json --pretty false",
    "lint": "node -e \"console.log('lint placeholder: @another-workbench/core')\"",
    "test": "pnpm exec vitest run"
  },
  "dependencies": {
    "@another-workbench/shared": "workspace:*"
  }
}
